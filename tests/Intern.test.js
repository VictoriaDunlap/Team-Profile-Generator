const Intern = require ('../intern')

describe ("Intern", () => {
    describe ("Intern", () => {
        it("should start the intern card", () => {
            const obj = new Intern()
          
            expect ("string" in obj).toEqual("string")
        })
        it("should return the intern school input", () => {
            const obj = new Intern(school)
          
            expect ("string" in obj).toEqual("string")
        })
        it("should start the intern role", () => {
            const obj = new Intern()
          
            expect ("string" in obj).toEqual("string")
        })
    })
})